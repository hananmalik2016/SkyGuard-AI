# Source code

## Planned modules

- `data/` — ingestion, validation, cleaning, missing-value handling
- `features/` — temporal, statistical, and multivariate features
- `detection/` — baseline QC and ML anomaly detectors
- `evaluation/` — metrics, validation, and experiment utilities
- `explainability/` — anomaly explanations and model interpretation
